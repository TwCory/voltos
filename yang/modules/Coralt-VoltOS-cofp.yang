module Coralt-VoltOS-cofp {

    grouping config-cofp-grouping {
        leaf table;
    }

    grouping config-ip-cofp-grouping {
        leaf accounting;
        leaf distributed;
        leaf linecard;
        leaf load-sharing;
        leaf optimize;
        leaf table;
        leaf traffic-statistics;
    }

    grouping config-ipv6-cofp-grouping {
        leaf accounting;
        leaf distributed;
        leaf load-sharing;
        leaf optimize;
    }

}